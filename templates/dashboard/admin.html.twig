{% extends 'layout/app.html.twig' %}

{% block title %}Administration - PAS{% endblock %}
{% block page_eyebrow %}Espace administrateur{% endblock %}
{% block page_title %}Vue d'ensemble de l'etablissement{% endblock %}

{% block app_body %}
    {{ include('dashboard/_alerts.html.twig') }}
    {{ include('dashboard/_stat_tiles.html.twig') }}

    <section class="content-grid content-grid-2">
        {{ include('dashboard/_recent_incidents.html.twig') }}
        {{ include('dashboard/_recent_transfers.html.twig') }}
    </section>

    <section class="content-grid content-grid-2">
        {{ include('dashboard/_today_activities.html.twig') }}

        <article class="panel">
            <h2>Administration</h2>
            <p>Gestion des comptes, des referentiels (batiments, ailes, cellules) et consultation du journal d'audit.</p>
        </article>
    </section>
{% endblock %}
